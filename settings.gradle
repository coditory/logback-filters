plugins {
    id("com.gradle.enterprise").version("3.11.1")
}

rootProject.name = "logback-filters"

gradleEnterprise {
    buildScan {
        termsOfServiceUrl = "https://gradle.com/terms-of-service"
        termsOfServiceAgree = "yes"
    }
}
